[package]
name = "manifold-fidget"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "SDF/angle-field backend (fidget-based Tree fields plus mesh-derived SDFs) used by manifold-gui for visualization — see NON_PLANAR_SLICING.md and MESH_SDF_VISUALIZATION.md."

[dependencies]
glam.workspace = true
fidget = "0.5"
